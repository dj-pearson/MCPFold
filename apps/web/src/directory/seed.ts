/**
 * The curated directory now lives in @mcpfold/core (S13.5) as the single source shared by the web
 * app and the public marketing-site directory. Re-exported here so existing imports keep working.
 */
export { DIRECTORY, DIRECTORY_VERSION, searchDirectory, type DirectoryEntry } from '@mcpfold/core';
