# PROGRESS — mcpfold build log

Append-only. One entry per story state change, newest at the bottom. A fresh loop reads
the tail of this file plus `prd.json` (story `status` fields) to pick the next eligible
story. **Never edit past entries** — only append.

Entry format:

```
[<ISO date>] <id> <status> — <one-line summary> (commit <sha>) [follow-ups: …]
```

Statuses: `todo` → `in_progress` → `done` (or `blocked`).

---

[2026-07-07] S0.1 done — pnpm monorepo scaffold: tsconfig.base (strict + noUncheckedIndexedAccess), eslint w/ core-purity no-restricted-imports, prettier, vitest workspace, packages/core smoke test. verify_all green.
[2026-07-07] S0.2 done — GitHub Actions CI: ubuntu/macos/windows × node 20 matrix (install/lint/typecheck/test+coverage/build) + dedicated core-purity + format:check job. Branch-protection docs in docs/ci.md.
[2026-07-07] S0.3 done — Ralph loop harness: ralph/PROMPT.md (iteration_steps + global DoD), PROGRESS.md (this file), AGENT_NOTES.md (path/fixture/secret/determinism decisions). Loop can identify next story from files alone.
[2026-07-07] S1.1 done — Zod canonical schema (schema.ts): Server/Profile/Config, transport↔command/url refine, path-required refine, strict unknown-key rejection, SecretRef regex. Types inferred in types.ts. 13 tests.
[2026-07-07] S1.2 done — JSONC loader (load.ts): loadConfig returns discriminated result with positioned (line/col) parse + pathed schema errors and fix hints; loadConfigOrThrow convenience. Spec §4 example (w/ comments) loads. 8 tests.
[2026-07-07] S1.3 done — Secret-ref grammar (secret-ref.ts): parseSecretRef (whole-string), findSecretRefsInString (embedded), findSecretRefs(server) over auth.token/headers/env; unknown scheme → known:false (no crash). Pure. 9 tests.
[2026-07-07] S1.4 done — Resolution engine (resolve.ts): resolveProfile/resolveAll (tag-intersection fold, stable sort), resolveProfileWithDiagnostics (unmatched tags, empty), UnknownProfileError. Secrets stay as refs. ResolvedServer exported. 8 tests.
[2026-07-07] fix(ci) — CI run #1 failed: `pnpm -r test --coverage` needs @vitest/coverage-v8 (local `pnpm test` omitted --coverage so it passed). Added @vitest/coverage-v8 devDep; exact CI command now green (95.94% core coverage).
[2026-07-07] S0.8 done — Deterministic output: core serialize.ts (sorted keys, preserved array order, 2-space, trailing \n; shuffle-invariant + property test) + check.ts drift engine (checkRendered/hasDrift). CLI runSyncCheck (sync --check → EXIT.DIFF on drift). Invariant documented (docs/cli-contract.md, AGENT_NOTES).
[2026-07-07] S0.6 done — Error hierarchy + redaction: core errors.ts (McpfoldError codes; UnknownProfileError refactored into it). CLI util/redact.ts (Redactor: ref-path + sentinel scrubbing; redactConfig), util/debug.ts (--debug/MCPFOLD_DEBUG, redacted, stderr), commands/diagnose.ts (leak-free bundle). Proven leak-free by test.
[2026-07-07] S0.10 done — Stable --json + exit codes: created packages/cli (mcpfold/mcpf bin, commander). output/exit-codes.ts (0/1/2), envelope.ts (versioned {ok,command,data,warnings,errors}), render.ts (human/JSON paths separated). Conformance + inline-snapshot tests. docs/cli-contract.md.
[2026-07-07] note — packages/cli created here (hosts S0.6/S0.8/S0.10 CLI contracts); S3.1 builds the fuller command surface on it. Cross-pkg @mcpfold/core resolved via tsconfig paths (typecheck) + vitest alias (test) so CI's test-before-build order works; build stays topological on dist. E2E-caught bug: commander routes --json to root even after subcommand → merge with || not ??.
[2026-07-08] S2.1 done — Adapter framework (packages/adapters): ClientAdapter/RenderedFile/SecretStrategy types, registry (register/get/require/list/clear), shared mcpServers helpers (toMcpEntry/toMcpServersShape/fromMcpServersShape + createMcpServersAdapter factory), cross-platform paths.ts (expandHome/userConfigDir/joinFor, win32/darwin/linux, injectable OsContext). 17 tests.
[2026-07-08] S2.2-S2.7 done — Six client adapters: cursor (mcpServers, shim), claude-desktop (user-only, needsRestart), claude-code (+type field), vscode (root `servers` + ${input:} native-input secrets — trap regression test), windsurf (mcp-remote wrapper for authed remotes, reversible parse), zed (root context_servers). Per-OS resolvePath tested; golden file-snapshots committed. registerAll() wires all six.
[2026-07-08] S2.8 done — Cross-adapter matrix harness: single canonical.jsonc folded through all six adapters → committed goldens (fixtures/matrix/*), render→parse round-trip invariant per adapter. Update goldens via `pnpm --filter @mcpfold/adapters test -u` (never -u in CI).
[2026-07-08] S1.6 done — Drift-diff engine (core/diff.ts): diffRendered(expected, actualContents|undefined, parser) → structured ConfigDiff (added/changed/unmanaged + field changes), semantic (key-order/whitespace-insensitive via serialize), fileMissing distinct, pure. Adapter-agnostic via minimal ParseCapable interface (no circular dep). 5 tests.
[2026-07-08] S3.1 done — CLI skeleton (cli.ts): commander framework, global flags --profile/--cwd/--dry-run/--json/--debug (merged before/after subcommand), --version, help lists init/import/add/sync/diff/doctor/secret/run + stubbed login/push/pull, unknown cmd → exit 2 (exitOverride). mcpf alias via bin map. Output routed through injectable writer.
[2026-07-08] S3.5 done — sync (commands/sync.ts + io/atomic-write.ts + io/backup.ts): resolve profiles → adapter.render → timestamped backup → atomic write (temp+rename, cleanup on fail). --profile, --dry-run (preview via S1.6), --check (CI gate exit 1), idempotent unchanged detection, restart hints. Injectable OsContext/clock. 6 tmp-dir integration tests + e2e-driven built binary.
[2026-07-08] S3.6 done — diff (commands/diff.ts): per-client added/changed/unmanaged + missing-file, --json structured, exit 1 on drift / 0 clean. 3 tmp-dir tests. E2E verified: diff(missing)→sync→diff(clean) with cursor mcpServers + vscode servers/inputs.
[2026-07-08] S3.2 done — init (commands/init.ts): scaffolds commented mcp.config.jsonc with $schema line + sample server/profile (validates itself), detects installed clients via adapter resolvePath existence (util/detect-clients.ts rewritten to use ALL_ADAPTERS), refuses overwrite without --force, --dry-run. Added optional `$schema` key to ConfigSchema (core). 6 tests.
[2026-07-08] S3.7 done — doctor (commands/doctor.ts + checks/*.ts): pathed config-validity errors, unpinned @latest (warn+pin fix), hardcoded secrets in env/headers (error+${env:} fix), unknown secret schemes (warn), VS Code mcpServers-vs-servers on-disk trap (error). Each finding {severity,file,where,message,fix}. Exit 2 on error-severity, --json. 6 tests.
[2026-07-08] S3.3 done — import (commands/import.ts): scans detected clients, adapter.parse → merge (dedupe by transport+command/url signature, union tags), per-client profiles, conflicts reported (not silently merged), hardcoded secrets rewritten to ${env:...} placeholders (not copied). --dry-run/--force. 5 tests. E2E: init→import(secret rewritten, no leak)→doctor.
[2026-07-08] S0.9 done — Offline/degraded contract (docs/offline-contract.md + resolver design): fail-closed resolution (no partial/blank secret ever injected; SecretResolutionError names provider+ref, exit 2), per-provider timeout (default 10s, AbortSignal) so hung providers abort cleanly, secret-free ops (validate/diff/doctor) work offline. Resolver interface carries the contract from the start. degraded.test.ts covers timeout/error/offline + offline validate/diff.
[2026-07-08] S4.1 done — SecretProvider interface + resolver (packages/secrets/resolver.ts): registry keyed by scheme, resolveSecrets(servers, {providers,timeoutMs}) injects values into token/headers/env refs (embedded refs supported), per-run memoization (call-count proven), missing-provider → structured error. Lives in packages/secrets (impure), injected into pure core.
[2026-07-08] S4.2 done — env + dotenv providers: env resolves ${env:NAME} from process.env (missing → clear error); dotenv reads a designated .env via minimal parser WITHOUT polluting process.env (leak-tested). defaultProviders(cwd) = [env, dotenv(<cwd>/.env)]. 8 tests.
[2026-07-08] S4.6 done — Per-adapter secret strategies (sync/strategy.ts): shim → secret-bearing servers rewritten to {command:'mcpfold',args:['run',name]} (no token/url/ref on disk); native-input → adapter emits ${input:} (vscode); inline → resolve+write ONLY if gitignored (spawnSync git check-ignore, injectable), else refuse (InlineNotIgnoredError) + loud warning. sync/diff now async, render via strategy. Test proves no raw secret in shim/native-input files. 6 tests.
[2026-07-08] S4.7 done — mcpfold run <name> (commands/run.ts): loads config, resolves the server's refs via providers (fail-closed, no leak), applies pin (@latest→@pin), injects env, execs stdio (stdio:inherit) or bridges remote via mcp-remote with resolved Authorization header; forwards SIGINT/SIGTERM; propagates child exit code. Injectable Spawner. 5 tests.
[2026-07-08] S4.8 done — secret set|test (commands/secret.ts): `test <ref>` resolves + reports ok/fail with value MASKED (never in human/data); `set <ref> --value` stores dotenv to .env (masked), env→export instructions, others deferred. Wired into CLI (run + secret subcommands). 5 tests. E2E: shim sync writes mcpfold-run launcher (no token), secret test masks value.
[2026-07-08] S9.3 done — Output redaction + backup hardening: redact.ts maskTokens (known prefixes ghp_/sk-/xox/AKIA/... + high-entropy letter+digit blobs, URL-safe class so it stops at separators; leaves refs/URLs/prose alone) folded into Redactor.string; diff --json + doctor findings run through Redactor.deep; backup.ts chmod 0600 on POSIX; .gitignore adds *.mcpfold.bak.*/*.mcpfold.tmp. Tests: maskTokens, backup perms, masked output.
[2026-07-08] S1.5 done — JSON Schema (packages/schema): generate.ts zodToJsonSchema(ConfigSchema) → committed mcp.config.schema.json ($id v1.json). Drift check (committed === generated, WRITE_SCHEMA=1 to regen) + ajv validates spec §4 example, rejects bad version/unknown key/hardcoded token. README documents $schema line; init SCHEMA_URL aligned to /schema/v1.json. prettier ignores the generated file.
[2026-07-08] fix(ci) — Windows CI failure on c79e252: git checked out generated mcp.config.schema.json as CRLF but generateSchemaText emits LF → drift check byte-mismatch. Added .gitattributes (* text=auto eol=lf) + CRLF-robust drift comparison. Cross-OS matrix caught it.
[2026-07-08] S9.1 done — Suite-wide secret-leak harness (security/ package, on CI matrix): SENTINEL injected via fake provider; scanForSentinel walks all files. Asserts ZERO leak for shim + native-input across all 6 adapters (sync + render); inline value confined to a gitignored target (refuses otherwise); `run` injects sentinel into child env ONLY (memory-only, no temp/cache file); push guard findRawSecretsForPush/assertRefOnlyForPush rejects raw high-entropy values in secret positions; meta-test proves the scanner detects a planted leak. sync/diff gained injectable providers. 8 tests.
[2026-07-08] S5.1-S5.4 done — E5 proxy (the moat, packages/proxy): S5.1 jsonrpc framing + stdio/memory transports + connectProxy (faithful bidirectional passthrough: ids/notifications/errors preserved). S5.2 filter.ts allow/deny (order-stable, schemas intact) + proxy filters tools/list responses + rejects tools/call to filtered tools with MCP error -32601 (20→3 count assertion). S5.3 run.ts shouldUseProxy + proxy stdio path (piped, connectProxy with server.tools); stream-level e2e: client sees only allowed tools (20→2); no directive → plain passthrough. S5.4 context-benchmark.ts + docs/benchmark.md: 45 tools→9, ~80% token cut (7476→1497); `pnpm --filter @mcpfold/proxy bench` regenerates. 22 tests (proxy 12 + cli run-filter 4 + ...).
[2026-07-08] S4.3-S4.5 done — Remaining secret providers (packages/secrets): S4.3 infisical (${infisical:env/folder/KEY}, machine-identity token from INFISICAL_TOKEN/PROJECT_ID, injectable fetchSecret → v3 raw-secret API; clear missing-token/project/secret errors). S4.4 keychain (per-OS keychainCommand: macOS security / Linux secret-tool / Windows PowerShell CredentialManager; injectable exec; actionable backend-unavailable + no-entry errors). S4.5 op (${op:vault/item/field} → op read; injectable exec; detects missing/locked CLI, never logs value). Shared exec.ts. defaultProviders now registers all 5 (fail-closed with actionable message if unconfigured). docs/secrets.md documents setup. 18 new tests (242 total).
[2026-07-08] S0.7 done — Schema versioning + migrate (core/migrate/): SCHEMA_VERSION=1, MIGRATIONS registry (N→N+1) with a worked 1→2 example (tags→labels), migrateConfig runner (sequential, throws on newer-than-target/missing step), detectVersion. loadConfig now errors "newer than this mcpfold supports" + upgrade hint for version>1. CLI `mcpfold migrate` (no-op at current version; backup+atomic-write when upgrading, --dry-run). 11 tests.
[2026-07-08] S0.4 done — Changesets + release: .changeset/config.json (publishable @mcpfold/* + mcpfold; ignore security/web/edge), release.yml (publish-dry-run job: build+publint+`pnpm -r publish --dry-run`; release job: changesets/action on main w/ NPM_TOKEN + provenance). Root scripts changeset/version-packages/release. repository/homepage/bugs + exports added to publishable packages; publint clean. Initial-release changeset. NEEDS: NPM_TOKEN repo secret to actually publish.
[2026-07-08] S0.5 done — Governance + adapter on-ramp: LICENSE (MIT, cloud excluded), NOTICE (no implied MCP endorsement), CONTRIBUTING/CODE_OF_CONDUCT/SECURITY (private disclosure), .github/ISSUE_TEMPLATE/* (bug/feature/adapter_request + config), PULL_REQUEST_TEMPLATE, CODEOWNERS (adapters/core/security routed), docs/adapters.md tutorial. `mcpfold scaffold-adapter <name>` (commands/scaffold-adapter.ts): generates compiling adapter module + test + fixture dir (factory-based, id cast, shim), prints CLIENT_IDS/ALL_ADAPTERS checklist; refuses invalid names/overwrite/outside-repo. 4 unit tests + e2e verified.
[2026-07-08] S3.4 done — add (commands/add.ts): `mcpfold add <name>` guided/scripted server entry. --url → http (or --transport sse) server; --package → stdio npx server (--pin fixes version). Tokens accepted ONLY as ${scheme:path} refs (--token-ref, isSecretRef guard rejects raw values loudly); --auth-type bearer|header|none; --tag. Insert via jsonc-parser modify/applyEdits so surrounding comments survive, then re-validate before atomic-write; --dry-run previews. Interactive path via injectable Prompt (default readline), duplicate-name + no-config UsageErrors. 7 tests (comment preserved, raw-token rejected, dup rejected, interactive, dry-run).
[2026-07-08] S8.2 done — Cross-platform lifecycle e2e (e2e/ workspace pkg on the CI matrix): full import → sync → diff(clean) → mutate → diff(drift) in an isolated tmp HOME using the REAL platform's path resolution, so ubuntu/macOS/windows each exercise their own client paths. Plants a Cursor config with a hardcoded secret in a stdio server's env → import rewrites it to ${env:} (canonical never contains the value) → sync writes + timestamped backup → clean diff exit 0 → asserts no secret VALUE in mcpfold OUTPUT (backups excluded: user's own data) → mutate → drift diff exit nonzero. Plus idempotent-second-sync (no new backup) + per-OS path assertion. e2e registered in pnpm-workspace.yaml + vitest.workspace.ts; tsconfig paths + vitest aliases to all @mcpfold/* pkgs. 3 tests.
[2026-07-08] S8.1 done — Docs site + README + hosted schema: new docs/index.md (landing LEADS with the ~80% context-window benchmark, not sync), docs/quickstart.md (install→init→import→add→sync→diff→doctor), docs/config-format.md (full mcp.config.jsonc reference generated-from-zod: top level, servers, auth, secret refs, tool curation, profiles, versioning). README reworked to lead with the context-window tax. scripts/build-docs.mjs (zero-framework, marked): renders all docs to dist-docs/ static site w/ nav+theme, LINK-CHECKS every internal link + heading anchor (fails build on breakage — caught a real broken #redaction anchor in cli-contract.md), stages the generated JSON schema at /schema/v1.json (+CNAME mcpfold.com, .nojekyll). `pnpm docs:build`/`docs:check`. CI: new `docs` job runs docs:build as a gate; .github/workflows/docs.yml deploys to GitHub Pages on main (schema served at stable URL init already references). Added marked devDep. NEEDS (user, one-time): enable GitHub Pages (Settings→Pages→Source=GitHub Actions) + point mcpfold.com DNS at Pages to make https://mcpfold.com/schema/v1.json live.
[2026-07-08] S6.1 done — Self-hosted Supabase (Coolify) + migration tooling (supabase/): config.toml, migrations/0001_init.sql (extensions schema + pgcrypto/uuid-ossp + guarded anon/authenticated/service_role roles + grants; idempotent, no-op on a real Supabase DB), seed.sql. scripts/migrate.sh = dependency-free `supabase db push`-style runner (psql-only, applies migrations/*.sql in order exactly once, tracked in public.schema_migrations, each migration+ledger insert in one txn; SEED=1 applies seed). scripts/smoke-test.sh asserts bootstrap + idempotency. docker-compose.ci.yml (Postgres for CI + local schema work) and docker-compose.coolify.yml (full Postgres/GoTrue/PostgREST/Realtime + Kong gateway, kong.yml) — the deploy artifact. .env.example documents every secret (POSTGRES_PASSWORD/JWT_SECRET/ANON+SERVICE keys/REALTIME_* /GitHub OAuth) w/ openssl gen commands; secrets from Coolify env/Infisical, never committed (.env* gitignored, only .env.example tracked). CI: new `db integration` job stands up the compose, migrates, smoke-tests. docs/self-hosting.md (Coolify deploy + migration workflow + secrets + local/CI testing). VALIDATED LOCALLY against a real Postgres 16 (initdb cluster): migrate+smoke+idempotency+seed all pass. Unblocks E6 code (S6.2 schema/RLS next). NEEDS (user): deploy the Coolify compose on the Contabo VPS + set real secrets; the CODE/migrations are testable in CI without it.
[2026-07-08] S6.2 done — DB schema + RLS (supabase/migrations/0002_schema.sql): tables users/teams/team_members/machines/configs. configs = append-only, versioned, JSONB canonical config with REFERENCES ONLY. Portable auth shims (auth.users stub + auth.uid() reading app.current_user_id GUC) created only when absent, so RLS is testable on plain-Postgres CI and a no-op on real Supabase. RLS on every table isolates by owner/team membership (is_team_member security-definer helper avoids policy recursion); no configs UPDATE policy. Immutability: BEFORE UPDATE trigger raises (append-only; DELETE left to FK cascade); configs_set_version trigger auto-numbers per config line (unique index on coalesce(team_id,owner_id),version). config_is_ref_only(jsonb) CHECK rejects raw tokens (ghp_/github_pat_/sk-/xox/AKIA anywhere) + non-${scheme:path} auth.token. config_audit view = who changed a team's config, when. Grants to authenticated (RLS-gated) + service_role (bypassrls). New scripts/test-rls.sh proves: A can't read B's configs, UPDATE rejected (RLS 0-rows + trigger), raw token rejected / ref accepted, audit shows 2 auto-numbered team versions. Wired into CI `db` job. VALIDATED LOCALLY against real Postgres 16 (all 7 assertions pass). docs/self-hosting.md gains a Data model section. Unblocks S6.3 (auth edge fn). 49/65.
[2026-07-08] S6.3 done — Device-code OAuth edge function (services/edge, new Deno service — first E6 code). RFC 8628-style flow in functions/auth-device/index.ts (routes matched by last path segment so it works mounted at /auth-device/* on the side service or /functions/v1/auth-device/* on managed Supabase): POST start → device_code(43-char b64url secret)+user_code(XXXX-XXXX, unambiguous alphabet)+verification_uri; POST approve (ONLY authenticated endpoint — verifies the browser user's GoTrue JWT via shared JWT_SECRET, binds user_id); POST poll → 200 tokens once approved / 400 authorization_pending / 400 expired_token (single-use claim, atomic UPDATE…WHERE status='approved'); POST refresh → fresh access token; GET health. lib/jwt.ts = zero-dep HS256 sign/verify via Web Crypto (access token carries sub+role:authenticated+aud:authenticated+scope+exp, so PostgREST+RLS accept it like GoTrue's). lib/codes.ts (injectable RNG for deterministic tests) + sha256Hex. Migration 0003_device_codes.sql: device_codes + sessions tables storing ONLY hashes of device_code/refresh_token, deny-by-default RLS (service_role only). CLI token-store (packages/cli/src/cloud/token-store.ts, AC#4): persists the session ONLY in the OS keychain (macOS security / Linux secret-tool via stdin / Windows CredentialManager), never a plaintext file (structural — no fs import); injectable backend; `mcpfold login` (S6.6) will wire it. Tests: deno unit (8: jwt tamper/expiry/wrong-secret, code format+determinism, sha vector) + deno integration auth.test.ts (10 steps: full start→approve→poll→refresh round trip against the live CI Postgres, single-use, expired, unauthorized-approve 401, unknown-code 404) + 4 vitest token-store. CI `db` job gains Deno setup + `deno test` against the migrated compose DB. services/edge excluded from pnpm eslint/prettier (deno fmt/lint own it). VALIDATED LOCALLY against real Postgres 15 (migrate + all deno tests green; full pnpm verify_all green). Refresh rotation/reuse-detection + per-machine revocation deferred to S9.5 (documented). Unblocks S6.4 (push/pull), S7.1 (web scaffold). 50/65.
[2026-07-08] S6.4 done — push/pull sync edge functions (services/edge, config-only, refs preserved). functions/push/index.ts: authenticated (Bearer access token → verifyJwt → sub); validateConfigForPush (lib/refguard.ts) rejects any raw secret value (ghp_/github_pat_/sk-/xox/AKIA regex mirroring the 0002 DB CHECK, + non-${scheme:path} auth.token) with 400 raw_secret BEFORE any write; inserts a new append-only version (${json}::jsonb) via the DB version trigger — never overwrites; RLS-denial→403, CHECK breach→400. functions/pull/index.ts: latest by default or ?version=N (POST body too); jsonb normalized (parse if driver returns text). Both run every query AS the user via lib/rls.ts asUser (sql.begin → set_config app.current_user_id + set local role authenticated) so S6.2 RLS is GENUINELY enforced, not bypassed — cross-tenant pull→404. lib/http.ts shared json/readJson/segment/authenticate. main.ts now a router (createRouter) dispatching /push,/pull else auth-device. Conflict strategy = last-write-wins + full version history (documented in README; CLI diffs via S1.6). Tests: deno sync.test.ts (7 steps against live CI Postgres: auth-required 401, append v1→v2, pull latest+ref intact, pull specific version, raw-secret rejected writing-nothing, cross-tenant B→404, B isolated) + refguard unit (5) — 15 deno tests / 17 steps green. CI `db` job's existing `deno test` picks them up (no workflow change). VALIDATED LOCALLY against real Postgres 15. DoD met: config round-trips machine→cloud→machine with ${env:} refs intact, no secret value ever stored. Unblocks S6.5 (side Docker service), S6.6 (CLI login/push/pull), S8.3. 51/65.
[2026-07-08] S8.3 done — Security review, telemetry opt-in, hardening. (1) Documented audit: docs/security.md (new, added to docs nav) — table of every value-exposure surface (rendered files/backups/CLI output/diagnostics/push/resolution) → its control (S4.6 strategies, S9.3 redaction+chmod, S0.6 diagnostics, S6.4/S9.1 push guards, memory-only resolution), noting the invariant is machine-verified by the S9.1 leak harness, not just asserted. (2) Pin-at-fold: hoisted @latest→@pin rewrite into renderWithStrategy (sync/strategy.ts) so EVERY rendered client file launches the pinned version (previously only run.ts/shim applied it); snapshot-safe (adapter goldens render directly + set no pin). strategy.test.ts proves pinned server renders @1.4.2 not @latest; doctor.test.ts already flags unpinned (AC's other clause). (3) Telemetry (packages/cli/src/telemetry.ts): OFF by default, opt-in ONLY via MCPFOLD_TELEMETRY=1; DO_NOT_TRACK=1 / =0 force off (opt-out wins); fixed allow-listed non-secret payload (command/cliVersion/nodeVersion/os/exitCode/durationMs — no paths/config/urls/secrets), redacted before send as final guard; injectable sink (no default network). 7 tests. (4) CI gate: .github/workflows/security.yml — gitleaks working-tree scan (--no-git, pinned zricethezav/gitleaks:v8.30.1, .gitleaks.toml allowlists test fixtures/sentinels/${scheme:path} refs/CI constants) + pnpm audit --prod --audit-level=high. (5) Threat model (docs/security.md): shim launcher (config-as-code→S9.2, fail-closed secrets, signal forwarding), curation proxy (off-by-default, tool-list only), sync channel (JWT+RLS, refs-only ×3 layers, refs-only-at-rest→S9.4). VALIDATED LOCALLY: gitleaks v8.30.1 clean on the tree, pnpm audit --prod clean, full pnpm verify_all green (docs/lint/typecheck/test/build), reworded one edge comment that tripped a generic-api-key false positive. Unblocks S8.4 (OSS launch). 52/65.
[2026-07-08] S8.4 done — OSS launch readiness. Post-install smoke (scripts/pack-smoke.mjs, `pnpm smoke`): packs all 6 publishable pkgs with pnpm pack (rewrites workspace:*→concrete version), installs the tarballs TOGETHER into a clean throwaway project, and drives the PUBLISHED bin (node node_modules/mcpfold/dist/bin.js) through --version→init→doctor→sync --dry-run — catches broken bin/`files`/dep before npm. Cross-platform: packer prefers on-PATH pnpm (CI) else corepack pnpm@10.33.0 (bare dev machine); external shims run with shell:true, bin via process.execPath. Wired into release.yml publish-dry-run job (after the existing publint + `pnpm publish --dry-run`). Launch collateral docs/launch.md: awesome-mcp PR blurb, Show HN/dev.to/r/mcp draft + WG-thread draft — ALL lead with the ~80% context-window benchmark (45→9 tools, 7476→1497 tokens), respect the no-implied-endorsement rule; pre-launch checklist. Root CHANGELOG.md (human summary; per-pkg changelogs are changesets-generated) + initial-release changeset now references the benchmark. VALIDATED LOCALLY: pack-smoke green (all 6 tarballs install + published binary runs the core loop); docs:check (12 docs) + lint + format clean. NOTE (unchanged from S0.4): actual npm publish still needs the NPM_TOKEN repo secret; smoke covers init→doctor→sync (import needs client fixtures — covered by the S8.2 e2e). Milestone v0.3 complete. 53/65.
[2026-07-08] S9.4 done — Self-hosted at-rest + in-transit hardening. Encrypted backups (supabase/backup/): encrypt-backup.sh (pg_dump→gzip→openssl AES-256-CBC pbkdf2 iter200000; NO plaintext hits disk), restore-backup.sh (decrypt→psql into a fresh DB), both with an optional PG_RUN prefix so the pg client can run in-container to match server version. test-backup.sh proves the round-trip: ciphertext opaque (sentinel + schema names absent), wrong key can't decrypt, restore into a fresh DB recovers the probe row — wired into CI `db` job (runs pg client inside the compose container via MCPFOLD_PG_CONTAINER so pg_dump==server version; avoids the pg_dump18→pg15 transaction_timeout skew I hit locally). In-transit: SECURITY_HEADERS in services/edge/lib/http.ts (HSTS max-age=2y+includeSubDomains+preload, X-Content-Type-Options nosniff, X-Frame-Options DENY, Referrer-Policy no-referrer) on EVERY edge response; refactored auth-device to use the shared json/readJson/segment so it's covered too; headers.test.ts asserts them (CI, no DB). verify-hardening.sh: operator script asserting endpoint-is-HTTPS + HSTS-present + HTTP→HTTPS-redirect + LUKS volume (cryptsetup) + Postgres ssl=on, each input optional (validated locally against github.com: HTTPS+HSTS+301). docs/security-at-rest.md (new, in docs nav): threat statement ("DB/backup compromise exposes config metadata — names/URLs/ref PATHS — never secret values", the deliberate server-side-at-rest model), LUKS runbook, encrypted-backup ops, TLS/HSTS enforcement, verification checklist; cross-linked from security.md + self-hosting.md. VALIDATED LOCALLY: backup encrypt→restore round-trip green against real Postgres 15, deno suite 16 green, verify-hardening against a real HSTS site, gitleaks clean, lint/format/docs green. Unblocks nothing new directly; S9.5 (session hardening) still needs S7.5. 54/65.
[2026-07-08] S6.6 done — CLI login/push/pull (cloud sync from the CLI, consuming the S6.3 auth + S6.4 push/pull edge + S6.3 token-store). New packages/cli/src/cloud/: api.ts (CloudApi interface + httpCloudApi(endpoint, fetch) mapping the edge endpoints; injected so commands are testable without network), session.ts (resolveEndpoint via MCPFOLD_API_URL, requireSession→"Not logged in", getAccessToken auto-refreshes+persists a session within 60s of expiry / dead→"log in again", endpointError names the unreachable endpoint), refguard.ts (client-side assertRefOnly mirroring the server guard + DB CHECK — catches raw secrets the SCHEMA doesn't constrain, e.g. in env values). commands/login.ts (device-code: startDevice→print user_code+verification_uri→poll until approved→saveSession to OS keychain; injectable now/sleep/print), push.ts (loadConfig→assertRefOnly→getAccessToken→api.push refs-only, tagged w/ hostname()), pull.ts (api.pull→diff remote-vs-local via S1.6 diffRendered w/ an identity JSON parser→without --yes shows diff + exit DIFF; with --yes atomic-writes serialize(remote.config), refs preserved). Wired into cli.ts replacing the stubs (removed stub()+unused UsageError import); pull uses --config-version to avoid clashing with commander's global --version. 10 cloud.test.ts (mocked api + inMemory keychain): login stores session, rejected-code errors, push refs-only+machine tag, raw-secret guard sends nothing, logged-out errors, expired→refresh, pull diff-not-applied (exit DIFF)/applied-with-yes/nothing-to-pull/logged-out. cli 107 tests, full verify_all green. Built-binary smoke: help lists real login/push/pull, logged-out push/pull give clear errors + clean --json envelope. DoD (two-machine live sync): validated at component level (S6.4 edge integration vs live Postgres + these CLI cmds vs mocked endpoint); full live two-machine run needs the deployed edge service. Unblocks S9.2 (config-as-code TOFU/signing, p0). 55/65.
[2026-07-08] S9.2 done — Config-as-code: trust-on-first-use + version-integrity signing (p0). TOFU (trust/tofu.ts): executableSignature = sha256 of {command,args,pin} (the runnable surface); TrustGate (fileTrustGate at per-machine ~/.config|APPDATA/mcpfold/trust.json chmod 600, memoryTrustGate for tests); status new/changed/trusted; untrustedServers(config). `run` now GATES stdio exec — refuses an untrusted/CHANGED launch command (exit 2, clear "run `mcpfold trust <name>`"), so a synced/edited config can't silently execute code. New `mcpfold trust [name]` command approves (shows the command being trusted). `pull` surfaces untrusted launch commands and, on --yes apply, pre-approves them (documented: --yes reduces protection). Signing (trust/signing.ts): per-user HMAC-SHA256 key in the OS keychain (getOrCreateSigningKey); push signs the config (over core serialize, deterministic) + sends the signature; pull VERIFIES — a present-but-invalid signature is hard-REJECTED (tampering), unsigned/keyless → loud warning (rollout-safe). Edge persists it: migration 0004 adds configs.signature; push stores body.signature, pull returns it (edge sync.test round-trips it). Pin integrity (trust/integrity.ts): optional SRI `integrity` field added to ServerSchema (JSON schema regenerated); verifyPackageIntegrity → match/mismatch/malformed; doctor checkPinIntegrity flags a malformed hash. Existing run/run-filter/leak tests updated to pass a trusting gate (exported memoryTrustGate/TrustGate from mcpfold). 8 trust.test.ts (run refuses untrusted, approved runs, CHANGED re-gates, sign/verify+tamper, pull rejects bad signature, integrity match/mismatch/malformed, doctor flags malformed). VALIDATED LOCALLY: cli 115 tests + security 8 + e2e 3 + edge 16 (18 steps, incl. signature round-trip vs live Postgres 15 w/ 0004) all green; full verify_all green; gitleaks clean; built-binary smoke — `run playwright` refuses pre-trust (exit 2), `trust playwright` approves + writes the store. Milestone-critical p0 done. 56/65.
[2026-07-08] S6.5 done — Side Docker service for the edge functions (completes E6). Refactored the serve logic into services/edge/src/server.ts (createRouter now serves an explicit /health {ok,service} plus auth/push/pull; runServer adds GRACEFUL SHUTDOWN — SIGTERM/SIGINT → server.shutdown() drains in-flight + sql.end()); main.ts is now the thin entry re-exporting it. Dockerfile: FROM denoland/deno:alpine-2.8.2 (bumped from 2.1.4 which couldn't read lockfile v5), non-root deno user, COPY --chown + `deno cache main.ts`, .dockerignore excludes test/scripts, HEALTHCHECK polls /health via `deno eval` (NO --allow-* flags — eval runs all-perms; that was the bug that made the container unhealthy), CMD run --allow-net --allow-env. docker-compose.ci.yml (db:55433 + edge:8787, edge depends_on db healthy). scripts/mint-jwt.ts (signs a GoTrue-shaped token via lib/jwt for the smoke). scripts/container-smoke.sh: build+up --wait → migrate (host psql→55433) → seed user → asserts /health 200, unauth push→401, authed push→v1, authed pull returns config. New CI `edge-container` job (setup-deno + the smoke). docs/coolify-edge-service.md (in docs nav; Coolify Dockerfile deploy, env-only secrets, health/graceful-shutdown, TLS at proxy) cross-linked from self-hosting.md; edge README gains a Side Docker service section. VALIDATED LOCALLY: container-smoke green against the REAL built image (all 4 assertions), deno check/lint/fmt clean, 14 edge unit tests green, docs build green. E6 (cloud backend) COMPLETE. 57/65.
[2026-07-08] S7.1 done — Web app scaffold (apps/web, React+TS+Vite; first E7 code). Auth abstracted behind an AuthClient interface (auth/authClient.ts): supabaseAuthClient wraps @supabase/supabase-js GoTrue (VITE_SUPABASE_URL/ANON_KEY); a deterministic in-memory mockAuthClient swaps in when VITE_E2E=1 so the flow is testable with NO live Supabase. AuthProvider (context + useAuth, catches misconfig → shows a config error), ProtectedRoute redirects unauthenticated → /login, react-router shell (Login/Dashboard + Header sign-out). Reuses @mcpfold/core (imports Config + CLIENT_IDS at RUNTIME — dashboard renders the six client chips; no config types redefined in the frontend). Responsive light/dark theme (styles.css). Cloudflare Pages: wrangler.toml (pages_build_output_dir=dist) + .github/workflows/pages.yml (build always runs; preview deploy via cloudflare/pages-action GATED on the CLOUDFLARE_API_TOKEN secret — skipped, not failed, when absent). Playwright e2e (test/*.e2e.ts named so the vitest workspace never collects them; testMatch + webServer runs `vite` with VITE_E2E=1): unauth→/login redirect, sign-in→dashboard→sign-out, wrong-creds error. New CI `web-e2e` job (build core → playwright install chromium → e2e) + pages.yml build job. apps/web added to root eslint ignore (react app self-typechecks); tsconfig paths @mcpfold/core→src for typecheck (vite/build resolve the built dist, so build order = core then web). VALIDATED LOCALLY: vite build (108 modules) + web typecheck + 3 Playwright e2e green (real chromium), full pnpm lint/typecheck/build green, `pnpm -r test` unaffected (web has no test script → skipped). DoD (login on a Pages preview): flow works via mock e2e + the real supabase adapter; NEEDS (user): CLOUDFLARE_API_TOKEN/ACCOUNT_ID secrets + a Supabase GoTrue instance for the actual preview deploy + real login (same pattern as NPM_TOKEN). Unblocks S7.2/S7.4/S7.5 (+S9.6 which needs apps/web). 58/65.
[2026-07-08] S9.6 done — Web app security headers + supply-chain gate (p1). apps/web/src/security/headers.ts = single source of truth for the header set: strict CSP (default-src 'self', script-src 'self' — no inline/remote scripts, connect-src 'self' https://api.mcpfold.com, frame-ancestors 'none'), HSTS (2y+includeSubDomains+preload), X-Content-Type-Options nosniff, X-Frame-Options DENY, Referrer-Policy no-referrer, Permissions-Policy (camera/mic/geo/cohort off). renderHeadersFile() emits the Cloudflare Pages public/_headers (generated + committed; vite copies it to dist/_headers — verified). CSRF: SPA uses Bearer-token auth (Authorization header, no ambient cookie) + the API requires it → classic CSRF N/A (documented). apps/web/src/security/tokens.ts: generateInviteToken (256-bit base64url, expiring; single-use enforced server-side) + isInviteExpired for S7.6 invites. Tests (test/security.e2e.ts, run in the S7.1 web-e2e CI job): _headers drift-guard (===renderHeadersFile), every required header present, token entropy/uniqueness/expiry — 7 web e2e green total. docs/threat-model.md (new, in nav): consolidated all-surfaces model (shim launcher/proxy/sync channel/web console/at-rest/CI gate), cross-linked from security.md. Supply-chain gate: the S8.3 security.yml (gitleaks + `pnpm audit --prod --high`) already covers apps/web's prod deps — verified `pnpm audit --prod` clean incl react/supabase. VALIDATED LOCALLY: web typecheck + build (_headers in dist) + 7 Playwright e2e + docs build + full lint/format green. NEEDS (user): the deployed-preview header assertion runs post-Cloudflare-deploy (the local drift test guards the config Cloudflare serves verbatim). 59/65.
[2026-07-08] S7.2 done — Visual config editor (apps/web/src/editor/). editor/validation.ts runs the SAME @mcpfold/core ServerSchema the CLI uses (reuse — identical errors), transport-aware (stdio→command/args, http/sse→url), maps the transport↔command/url refine (empty issue path) to the right field, tokens ref-only via isSecretRef + schema (friendly message). ConfigEditor.tsx: loads config (api.getConfig), CRUD servers (add validated draft / remove), live validation disables Add on any error, Save→api.saveConfig (push, new append-only version)→version + session history, OPTIMISTIC version/history with rollback on error. api/cloud.ts: CloudApi (getConfig/saveConfig) — real fetch to the edge /pull,/push with the Supabase Bearer token (VITE_API_URL) + a module-singleton in-memory mock for VITE_E2E. Wired /editor protected route + Header nav (Overview/Editor/Sign out). Tests (test/editor.e2e.ts, in the web-e2e job): add a stdio server, raw token blocked+Add disabled + ${env:} ref clears it, save→version 1+history; http transport requires a url (live schema). 9 web e2e green (3 auth + 4 security + 2 editor). VALIDATED LOCALLY: web typecheck + build + 9 Playwright e2e + full lint/format green. DoD: edits persist as new versions (mock-validated; real via S6.4 edge push) + validate identically to the CLI (shared schema). Unblocks S7.3 (profiles) + S7.6 (teams). 60/65.
[2026-07-08] S7.3 done — Profile management UI (apps/web/src/profiles/). profiles/validation.ts runs the shared @mcpfold/core ProfileSchema (path required for project/workspace via the refine → mapped to the path field). ProfileManager.tsx: loads config, CRUD profiles (client/scope/path/include-tags; path field shown only for non-user scope), LIVE PREVIEW of resolved servers via core resolveProfile on a synthetic config (server-level curation viz — chips per resolved server), save→push new version. Wired /profiles protected route + Header nav link. Tests (test/profiles.e2e.ts): seed a tagged server in the editor→save→create a profile including that tag→preview resolves to the server→add profile; project scope requires a path (Add disabled until filled, shared-schema error). 11 web e2e green (3 auth + 4 security + 2 editor + 2 profiles). VALIDATED LOCALLY: web typecheck + build + 11 Playwright e2e + lint/format green. DoD: profiles editable + fold preview correct (resolveProfile, same engine as CLI). Unblocks S9.5 still needs S7.5. 61/65.
