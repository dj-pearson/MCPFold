/**
 * @mcpfold/schema — the published JSON Schema for `mcp.config.jsonc`, generated from zod.
 */
export {
  generatePolicySchema,
  generatePolicySchemaText,
  generateSchema,
  generateSchemaText,
  POLICY_SCHEMA_URL,
  SCHEMA_URL,
} from './generate.js';
