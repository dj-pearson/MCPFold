/**
 * @mcpfold/schema — the published JSON Schemas (generated from zod): `mcp.config.jsonc` and the
 * org-managed `mcp.policy.jsonc` (S18.3).
 */
export { generateSchema, generateSchemaText, SCHEMA_URL } from './generate.js';
export { generatePolicySchema, generatePolicySchemaText, POLICY_SCHEMA_URL } from './generate.js';
