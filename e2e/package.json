{
  "name": "@mcpfold/e2e",
  "version": "0.0.0",
  "private": true,
  "description": "Cross-platform end-to-end CLI lifecycle suite (S8.2).",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "lint": "eslint . --max-warnings=0",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@mcpfold/core": "workspace:*",
    "@mcpfold/adapters": "workspace:*",
    "mcpfold": "workspace:*"
  }
}
