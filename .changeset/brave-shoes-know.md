---
'@mcpfold/adapters': patch
'mcpfold': patch
'@mcpfold/core': patch
'@mcpfold/proxy': patch
'@mcpfold/schema': patch
'@mcpfold/secrets': patch
---

Add npm READMEs and richer package metadata (keywords, author, engines) for every published package,
and add WinGet (`winget install PearsonMedia.mcpfold`) + Docker (`ghcr.io/dj-pearson/mcpfold`) as
install channels alongside npm, Homebrew, and Scoop.
